<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Arvioinnin luonti tai poistaminen</title>
    <link href= "/public/stylesheets/arviointi/style.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">
     <style>
         th { 
           color: #46007F;
           font-family: "Fantasy", Papyrus;
           font-weight: 900;
           font-size: 25px;
         }
        td {
           color: #8500FB;
           font-family: "Ink Free";
           font-weight: 900;
           font-size: 20px;
        }
       a {
          text-color: red;
          font-family: Segoe Print;
          font-weight: bold;
      }
     </style>

    <% if (messages.success) { %>
    <div class="alert alert-success" role="alert"><%- messages.success %></div>
    <% } %>

    <% if (messages.error) { %>
    <div class="alert alert-danger" role="alert"><%- messages.error %></div>
    <% } %>

    <div class="card"> 
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-pill active">
                    <a class="nav-link">Arvioinnin luonti tai poistaminen</a>
                </li>
                <li class="nav-pill ml-auto">
                    <a class="nav-link active" href="/Arviointi/add">Lisää Arviointi</a>
                </li>
            </ul>
        </div>
    <div class="card-body">
        
        <% if(data.length) { %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Paivamaara</th>
                    <th scope="col">Arvosana</th>
	    <th scope="col">idOpiskelija</th>
	    <th scope="col">idOpintojakso</th>
                    <th width="200px">Action</th>
                </tr>
            </thead>
            <tbody>
            <% for(var i = 0; i< data.length; i++) { %>
                <tr>
                    <th scope="row"><%= (i+1) %></th>
                    <td><%= data[i].Paivamaara%></td>
                    <td><%= data[i].Arvosana%></td>
	    <td><%= data[i].idOpiskelija%></td>
	    <td><%= data[i].idOpintojakso%></td>
                    <td>
                        <a class="btn btn-success edit" href="../arviointi/edit/<%=data[i].idArviointi%>">Edit</a>                       
                        <a class="btn btn-danger delete" onclick="return alert('Oletko varma, että haluat poistaa arvioinnin?')" href="../arviointi/delete/<%=data[i].idArviointi%>">Delete</a>                       
                    </td>
                </tr>
            <% } %>
            </tbody>
        </table>
        <% } %>
        
        <!-- if result is empty -->
        <% if(!data.length) { %>
            <p class="text-center">Arviointia ei löytynyt!</p>
        <% } %>

        </div>
    </div>
</body>
</html>